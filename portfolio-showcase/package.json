{
  "name": "my-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "server": "json-server --watch db.json --port 3001",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint ."
  }
}